Use an external CSS file to format your website;
- Modify your CSS so that your navigation bar uses CSS buttons;
- Use CSS to enable your HTML page to be "print friendly" (printed without a navigation bar, background colour/image removed and with a larger font size than displayed on the screen); Apply this to all pages on your site;
- Your pages must have an identical navigation bar on each page;
- Your pages must have a logo (any reasonable image will be OK);
- Your new last home page must have your contact details and a notice stating that you have permission to use or that you are the copyright holder of all content on your site. This content must be in the ‘footer’ (not necessarily the HTML5 element - but it would work) of the page using the copyright symbol;
- Modify your last resume page to contain a table (for example, units/subjects you will take as part of your degree or your previous employment). The table must have at minimum at least 4 rows with 3 columns.
- Printing working? use Ctrl+P -> print in the Style.CSS or Print.CSS file
- Did you use an external CSS file to format your website; > Use only an external CSS file to format your website (there should not be any internal or inline CSS style);
- Did you include a Logo/Icon (Topic 7)? A small logo has been added to each page so that an image appears on the tab on the browser to the left of the webpage title.